import styled from "styled-components";

const Counter = styled.div`
	position: relative;
	left: 16%;
	bottom: -13%;
`;
export const CounterHour = () => {
	return (
		<Counter>
			<p>Razem:</p>
		</Counter>
	);
};
