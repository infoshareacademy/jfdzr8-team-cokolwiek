import styles from "./Login.module.css"
export const Login = ({ setUserId, setAdmin }) => {
  return (
    <div className={styles.loginStyle}>
      <h2>Welcome to TimeTaker</h2>
      </div>
      )
  }